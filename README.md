# capstone
Supervised regression pipeline forecasting 30-minute workflow event volume. Progressed through 5 model iterations (Ridge → LightGBM), reducing test RMSE by 75% via feature engineering, hyperparameter tuning, and regularization. Python, LightGBM, scikit-learn.
